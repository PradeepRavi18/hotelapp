version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm install -g pnpm
      - ENV PNPM_HOME="/pnpm"
      - ENV PATH="$PNPM_HOME:$PATH"
      - pnpm install
run:
  command: pnpm start
  network:
    port: 3000
